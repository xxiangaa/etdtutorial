import React from 'react';
import cc from 'classcat';
function Attribution({ proOptions, position = 'bottom-right' }) {
    if ((proOptions?.account === 'paid-starter' || proOptions?.account === 'paid-enterprise') &&
        proOptions?.hideAttribution) {
        return null;
    }
    const positionClasses = `${position}`.split('-');
    return (React.createElement("div", { className: cc(['react-flow__attribution', ...positionClasses]), "data-message": "Please only hide this attribution when you have a pro account: https://pro.reactflow.dev/plans" },
        React.createElement("a", { href: "https://reactflow.dev", target: "_blank", rel: "noopener noreferrer" }, "React Flow")));
}
export default Attribution;
