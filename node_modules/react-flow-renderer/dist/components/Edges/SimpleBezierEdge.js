import React, { memo } from 'react';
import BezierEdge, { getBezierPath } from './BezierEdge';
export function getSimpleBezierPath(props) {
    return getBezierPath({ ...props, curvature: 0 });
}
export default memo((props) => {
    return React.createElement(BezierEdge, { ...props, curvature: 0 });
});
