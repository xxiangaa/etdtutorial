import React, { memo } from 'react';
import SmoothStepEdge from './SmoothStepEdge';
export default memo((props) => {
    return React.createElement(SmoothStepEdge, { ...props, borderRadius: 0 });
});
