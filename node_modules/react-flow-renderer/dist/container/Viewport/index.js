import React from 'react';
import { useStore } from '../../store';
const selector = (s) => s.transform;
function Viewport({ children }) {
    const transform = useStore(selector);
    return (React.createElement("div", { className: "react-flow__viewport react-flow__container", style: { transform: `translate(${transform[0]}px,${transform[1]}px) scale(${transform[2]})` } }, children));
}
export default Viewport;
